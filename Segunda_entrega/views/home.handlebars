<div>
    <h2>Listado de productos DESDE HOME.HANDLEBARS:</h2>
    {{#each products}}
        <div>
            <p>{{this.title}}</p>
            <p>{{this.description}}</p>
            <p>{{this.category}}</p>
            <p>${{this.price}}</p>
            <p>{{this.stock}}</p>
        </div>
    {{/each}}
</div>